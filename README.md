# Optical-Character-Recognition
This project implements Optical Character Recognition (OCR) to extract text from images using Python. The system processes image files, detects textual content, and converts it into machine-readable text. OCR can be used for digitizing documents, extracting information from scanned images, and automating text-based data entry tasks.
